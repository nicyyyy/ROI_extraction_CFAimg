

1. 程序运行之前，首先将ImageFunction.m文件中的picture1    ，picture2路径按照你自己的习惯修改一下；
2. 我所说的数字的状态，像“数字图库”文件夹下的样子这     样，保持一致；
3. ImageRecognition.m文件只是一个被调用的函数，不需要     修改；
4. 运行时，将两个.m 文件放在一个文件夹下，运行      ImageFunction.m文件即可，窗口即显示匹配结果。

   **自己可以看一下代码，也可以自己写一个，这个只做参考。